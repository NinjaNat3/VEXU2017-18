#pragma config(Sensor, dgtl2,  ,               sensorQuadEncoder)
#pragma config(Sensor, dgtl4,  ,               sensorQuadEncoder)
#pragma config(Motor,  port1,           claw,          tmotorVex393_HBridge, openLoop, encoderPort, None)
#pragma config(Motor,  port2,           shoulderR1,     tmotorServoContinuousRotation, openLoop, encoderPort, dgtl3)
#pragma config(Motor,  port3,           shoulderR2,     tmotorServoContinuousRotation, openLoop, encoderPort, dgtl4)
#pragma config(Motor,  port4,           shoulderL1,        tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port5,           shoulderL2,        tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port6,           rightDrive,    tmotorServoContinuousRotation, PIDControl, encoderPort, dgtl1)
#pragma config(Motor,  port7,           leftDrive,     tmotorServoContinuousRotation, openLoop, reversed)
#pragma config(Motor,  port8,           lift,         tmotorServoContinuousRotation, openLoop)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//
//this is a test comment
task main(){
	while(true){
		drive();
		liftShoulder()
		liftClaw()
		liftGoal()
	}
}

void drive(){
	motor[right] = vexRT[Ch2];
	motor[left] = vexRT[Ch4];
}

void liftShoulder(){
	if(vexRT[Btn7D]==1){
		motor[shoulderR1] = 40;
		motor[shoulderL1] = 40;
		motor[shoulderR2] = 40;
		motor[shoulderL2] = 40;
	} else if (vexRT[Btn7L] == 1]){
		motor[shoulderR1] = -40;
		motor[shoulderL1] = -40;
		motor[shoulderR2] = -40;
		motor[shoulderL2] = -40;
	} else{
		motor[shoulderR1] = 0;
		motor[shoulderL1] = 0;
		motor[shoulderR2] = 0;
		motor[shoulderL2] = 0;
	}
}

void liftClaw() {
	if (vexRT[Btn7U] == 1) {
		motor[claw] = 40;
	} else if (vexRT[BTN7R] == 1) {
		motor[claw] = -40;
	} else {
		motor[claw] = 0;
}

void liftGoal() {
	if (vexRT[Btn8U] == 1) {
		motor[lift] = 40;
	} else if (vexRT[Btn8R] == 1]){
		motor[lift] = -40;
	} else{
		motor[lift] = 0;
	}
}
